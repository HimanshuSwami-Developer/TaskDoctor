{
  "name": "task-doctor",
  "version": "1.0.0",
  "description": "Task Doctor — product modules, flows, screens, conditions and issues, stored in Neon Postgres",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cloudinary": "^2.11.0",
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "pg": "^8.23.0"
  }
}
